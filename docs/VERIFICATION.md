# 驗證與上線狀態

此紀錄區分本機已完成測試與連接實際帳戶後才可完成的事項。

## 已完成

- 31項真實PostgreSQL語義安全情境，使用PGlite執行兩份migration。涵蓋RLS、伺服器發布時間、不可提權、匿名／待審／停用隔離、最後管理員、版本衝突、封存／替換、操作記錄及可信匯入的最小資料表權限。
- 8項日期轉換及前端公開key邊界測試。
- 26次本機瀏覽器無障礙掃描，涵蓋登入、申請、密碼重設、學生、老師、管理頁及對話框，淺／深模式和320／390／1440像素。
- 材料新增、上課前一周09:00預設、封存／恢復、帳戶審批、最後管理員保護、對話框Tab／Escape及焦點還原、長中文、待審／停用畫面。
- 正式建置成功，公開產物掃描沒有教材、私密key或demo帳戶。正式build不包括demo資料。
- 教材52項、8.22MiB。學生PPT共218頁、508個內部關係通過檢查；43個來源檔雜湊未變。重建穩定，故意加入附件、巨集、隱藏頁、外連和破損關係的5種測試均拒絕。
- 本機Lighthouse行動裝置模擬：效能97、無障礙100；LCP約2.4秒、TBT 0毫秒、CLS 0.009。這是單次本機量測，不能代表正式網路或所有使用者的速度。

## 正式部署與資料服務驗證（2026年9月6日，香港時間）

正式入口：[公益粵語課堂學習門戶](https://tommyzhang316.github.io/cantonese-course-portal/)。GitHub Pages 部署工作流程 [33985131739](https://github.com/tommyzhang316/cantonese-course-portal/actions/runs/33985131739) 已成功；以下入口及資料檢查均直接連接正式服務，並非本機示範。

對正式 Supabase 專案完成68次只讀 HTTP 請求，20項檢查全部通過。HTTP 回應為58次200、6次預期的儲存庫拒絕400、4次預期的匿名資料表拒絕401。

- 8堂課的日期、18:00開課時間、120分鐘長度及排序均與材料清單一致：9月18日、10月2日、10月9日、10月16日、10月23日、10月30日、11月6日、11月13日。
- 正式資料庫有52份材料：26份定時發布、23份永不向學生發布、3份即時發布；匯入的材料均未封存。
- 全部26份定時材料的時間均為所屬課次提前7日、香港時間09:00；材料權限、檔案路徑、類型及大小與本機清單一致。
- `course-materials` 儲存庫的 `public` 值為 `false`。
- 使用可信匯入管理憑證只讀下載52份檔案，52份的 SHA256 及大小均與原清單一致，共8,621,518 bytes。此項證明雲端檔案完整，不等同已測試學生或教師的登入權限。
- 匿名讀取 `resources`、`profiles`、`lessons`、`audit_log` 均回應401；沒有返回資料列。
- 分別用一份教師專用材料及一份未到發布時間的材料，測試匿名公開網址、私人下載網址及簽名網址請求，均回應400；兩次目錄查詢均回應200及空陣列，沒有返回物件。
- 公開 Auth 設定回應200：電郵登入啟用、允許註冊、需要電郵確認、電話登入停用。這只是設定讀取，沒有發送郵件，也不代表 SMTP 已可用。

本機證據檔為 `.local/live-read-checks-report.json`；檢查腳本為 `.local/live-read-checks.mjs`。兩者均不隨公開網站或 Git 版本提交。檢查沒有修改伺服器資料、建立帳戶或發送郵件，也沒有輸出憑證。

## 正式公開入口驗收（2026年9月6日，香港時間）

使用全新、隔離的無頭 Chrome 檢查正式網址，沒有操作負責人的 Chrome 登入工作階段。範圍限於未登入入口及申請表顯示；所有非 GET／HEAD 請求均由檢查攔截保護，實際未出現任何提交請求。

- 23項入口檢查全部通過。依正式 HTML 與 JavaScript 的實際引用追蹤公開模組，11個 HTML／JavaScript／CSS／圖像檔案均回應200；可直接比較的6個靜態檔案均與本機已核對版本 SHA256 一致。正式部署的網址設定會改變 JavaScript 編譯檔名，因此不以本機舊檔名作為正式資源清單。
- 取得的7個文字類公開檔案未包含已知私人憑證、示範帳密或私人教材清單特徵。另檢查 `.env`、`.env.local`、`.local/ops.env`、私人教材清單及資料庫遷移檔案網址，5項均回應404。
- `?demo=1` 在正式網站仍顯示登入頁，不能開啟示範角色或示範帳戶。
- 登入及申請頁在1440、390、320像素寬度正常顯示，沒有橫向溢出或破圖；已肉眼檢視8張完整頁面截圖。
- 8次 axe 掃描涵蓋登入／申請、淺色／深色及上述尺寸，WCAG 2 A／AA、2.1 AA 規則沒有違規項。色彩對比在顯示模式轉場完成後量測。這是自動化掃描證據，不等同全面人工無障礙認證。
- 鍵盤第一個焦點為「跳到主要內容」連結。申請表的姓名、電郵、密碼及密碼確認欄均正常顯示，沒有提交表單。
- 瀏覽器 JavaScript 執行錯誤、console 錯誤、網路失敗及被攔截提交請求均為0。

本機證據檔為 `.local/live-public-report.json`；8張截圖位於 `.local/live-public-shots/`。明確取回公開檔案及探測私人路徑共16次 HTTP 請求，瀏覽器載入頁面的其他請求另作錯誤監測，沒有混入上述68次 Supabase 檢查。

## 正式角色權限及測試帳戶清理（2026年9月6日，香港時間）

使用5個有專屬標記的合成帳戶，透過正式 Auth API 分別登入，沒有發送郵件、使用真實師生身份或操作負責人的 Chrome 工作階段。帳戶包括已核准學生、已核准教師、停用學生，以及兩個待審學生；其中一個原為管理員驗收候選，這次始終未獲管理員權限。

- 建立帳戶時故意在使用者 metadata 填入 `admin`／`approved`，10項初步檢查全部通過：5人仍由資料庫初始化為 `student`／`pending`，且讀不到教材。
- 正式資料及儲存 API 共記錄208次 HTTP 請求，105項角色檢查全部通過；Auth 建立及獨立登入請求另行執行，沒有混入208次計數。
- 已核准學生只收到3份即時發布資料的記錄。對全部52份教材逐一實際下載：3份成功且大小與 SHA256 完全一致，其餘49份未到期或教師專用資料均被拒絕。
- 已核准教師可讀取全部52份教材記錄；52份實際私人下載的大小及 SHA256 均與清單一致。待審及停用帳戶收不到教材或課表，已知檔案路徑的下載也被拒絕。
- 所有測試身份均不能透過公開檔案網址、簽名網址或物件目錄繞過限制。非管理員的4類管理 RPC、直接修改自己角色及呼叫私人 schema 均被拒絕；教師亦不能取得其他帳戶名錄或操作記錄。
- 測試結束後，逐一核對 UUID、專屬 Auth marker、電郵及非管理員身份，已透過 Auth Admin API 刪除本批5個合成帳戶，並核對帳戶及關聯個人資料均無殘留。刪除後持原有 token 也讀不到教材。清理19項檢查全部通過。
- 這一階段沒有建立任何 QA 教材記錄或儲存物件；清理前後8堂課及52份教材記錄完全相同。操作記錄保留本次合成帳戶的驗收及刪除紀錄。

本機證據為 `.local/live-role-prepare-report.json`、`.local/live-role-read-roles-report.json` 及 `.local/live-role-cleanup-stage1-report.json`，均不提交到公開 Git。角色報告已附清理完成狀態；報告不含密碼、金鑰、token 或檔案內容。

## 仍待完成的正式驗收

- 第一位管理員完成自己的電郵驗證及登入，並確認管理員權限已正確啟用。
- 正式管理員 Auth 工作階段與管理操作驗收。學生、教師、待審及停用的正式服務權限已由上述合成帳戶驗證；沒有冒用真實師生身份，管理員候選這次沒有提升權限。
- 正式服務的短時發布前後邊界、封存／替換檔案，以及角色停用或降級後持既有 token 的即時撤權。本次只驗證既有發布政策及刪除合成帳戶後的 token 失效範圍；本機 PGlite 情境不取代這些正式服務測試。
- 正式註冊、確認信、密碼重設及 SMTP 收信流程。公開 Auth 設定只能確認功能開關，不能證明郵件已送達，也不能代表可向所有學生發信。
- 正式 Auth 回跳設定及完整登入流程由部署負責人完成確認；獨立入口檢查沒有提交註冊或測試郵件連結。
- 兩個真實資料庫連線的競態驗證，以及登入後的行動裝置教材下載驗收。

正式入口、私人材料服務及上述師生角色隔離已完成實際連線檢查；管理員、郵件及其餘未驗證流程仍須明確保留。
